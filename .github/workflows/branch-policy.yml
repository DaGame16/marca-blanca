name: Branch Policy

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]
    branches:
      - main
      - develop

permissions:
  contents: read

jobs:
  check-base-branch:
    runs-on: ubuntu-latest
    steps:
      - name: Validar origen del PR
        env:
          BASE_REF: ${{ github.event.pull_request.base.ref }}
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
        run: |
          echo "PR: $HEAD_REF -> $BASE_REF"

          if [ "$BASE_REF" = "main" ]; then
            if [ "$HEAD_REF" != "develop" ] && [[ "$HEAD_REF" != hotfix/* ]]; then
              echo "::error::No se permiten PRs directos a 'main' desde '$HEAD_REF'. Solo se acepta 'develop' o ramas 'hotfix/*'."
              exit 1
            fi
          fi

          if [ "$BASE_REF" = "develop" ]; then
            if [[ "$HEAD_REF" != feature/* ]] && [[ "$HEAD_REF" != bugfix/* ]] && [[ "$HEAD_REF" != hotfix/* ]] && [[ "$HEAD_REF" != docs/* ]] && [[ "$HEAD_REF" != chore/* ]] && [[ "$HEAD_REF" != refactor/* ]]; then
              echo "::error::La rama '$HEAD_REF' no sigue la convención esperada para PRs a 'develop' (feature/*, bugfix/*, hotfix/*, docs/*, chore/* o refactor/*)."
              exit 1
            fi
          fi

          echo "OK: origen de PR válido."
