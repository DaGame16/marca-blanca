/**
 * Vacio por ahora: no hay ninguna regla de negocio propia de "autenticacion"
 * todavia -- la unica regla real (verificar credenciales) vive en
 * Usuario.verificarCredenciales(), en usuarios-domain. Este modulo existe
 * por consistencia estructural (hexagonal estricto en todos los modulos),
 * no porque haya contenido real todavia.
 */
package com.marcablanca.platform.autenticacion.domain;
