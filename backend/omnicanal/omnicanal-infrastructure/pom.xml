<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.marcablanca.platform</groupId>
        <artifactId>marca-blanca-backend</artifactId>
        <version>0.1.0-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>omnicanal-infrastructure</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <dependency>
            <groupId>com.marcablanca.platform</groupId>
            <artifactId>omnicanal-application</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.marcablanca.platform</groupId>
            <artifactId>omnicanal-domain</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- ContextoEmpresaActual: el filtro del webhook fija el tenant aca,
             igual que hace JwtAuthFilter en autenticacion-infrastructure. -->
        <dependency>
            <groupId>com.marcablanca.platform</groupId>
            <artifactId>empresas-application</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- Solo el puerto de entrada publico de modulos-empresa, para el
             adaptador ACL PuenteModulosOmnicanal (gate del modulo). No se
             toca su modelo interno. -->
        <dependency>
            <groupId>com.marcablanca.platform</groupId>
            <artifactId>modulos-empresa-application</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <!-- Cifrado del token de LIWA en reposo (CifradorOmnicanal), mismo
             mecanismo que CifradorDeCorreo. -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-crypto</artifactId>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
